{
  "name": "monitor-api",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "storage": "*",
    "utils": "*",
    "yargs": "^17.7.2"
  },
  "devDependencies": {
    "@types/node": "^20.14.1",
    "typescript": "^5.4.5",
    "vitest": "^3.0.5"
  },
  "scripts": {
    "build": "yarn workspace utils build && yarn workspace storage build && rm -rf ./dist && tsc",
    "lint": "eslint .",
    "dev": "yarn build && node ./dist/main.js",
    "test": "vitest run __test__/*.test.ts"
  }
}
