{
  "name": "monitor-worker",
  "version": "1.0.0",
  "main": "dist/main.js",
  "dependencies": {
    "@arbitrum/sdk": "^4.0.0",
    "assertion-monitor": "*",
    "batch-poster-monitor": "*",
    "ethers": "^5.5.4",
    "monitor-core": "*",
    "retryable-monitor": "*",
    "storage": "*",
    "utils": "*",
    "yargs": "^17.7.2"
  },
  "devDependencies": {
    "@types/node": "^20.14.1",
    "typescript": "^5.4.5",
    "vitest": "^3.0.5"
  },
  "scripts": {
    "build": "yarn workspace utils build && yarn workspace storage build && yarn workspace assertion-monitor build && yarn workspace batch-poster-monitor build && yarn workspace retryable-monitor build && rm -rf ./dist && tsc",
    "lint": "eslint .",
    "dev": "yarn build && node ./dist/main.js",
    "test": "vitest run __test__/*.test.ts"
  }
}
